# Boogle
Boogle is a TUI implementation of the classic Boggle tabletop game in Java.

## Building & Running
Dependencies:
 - JDK Version >=8, with `javac` and `java` available in `$PATH`
 - Unix Make

### Building
```sh
make
```

### Running
```sh
make run
```
